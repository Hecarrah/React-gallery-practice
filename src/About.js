import './App.css';
import Header from "./components/Header"

function About() {
  return (
    <div className="App">
      <Header />
      <h1>This is just a practice project to get used to a more modern version,
        <br /> in fact this entire page is only to make sure that the router works correctly.</h1>
    </div>
  );
}

export default About;
